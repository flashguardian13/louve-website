<table>
  <tr>
    <td class="content-form">
      <%= form_for(@content) do |f| %>
        <%= render 'shared/error_messages', object: @content %>

        <div>
          <%= f.label :title %><br/>
          <%= f.text_field :title %>
        </div>

        <div>
          <%= f.label :content %><br/>
          <%= f.text_area :content %>
        </div>

        <div>
          <%= f.check_box :is_visible %>
          <%= f.label :is_visible, 'Make this post publicly visible' %>
        </div>

        <div>
          <%= f.submit 'Preview' %>
          <%= f.submit submit_text %>
        </div>

        <%= link_to 'Cancel', blog_posts_path %>
      <% end %>
    </td>

    <td class="content-preview">
      <%= render 'post', object: @content %>
    </td>
  </tr>
</table>
